using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pathfinder : MonoBehaviour
{
    [SerializeField]
    Transform[] waypoints;

	// Use this for initialization
	void Start ()
    {
		
	}
	
	// Update is called once per frame
	void Update ()
    {
		
	}

    //Raycast que diga si hay un obstáculo entre el enemigo y el jugador

    //Método que diga cual es el waypoint más cercano al jugador

    //Método que diga cual es el waypoint más cercano del enemigo

    //Método que empieze la ruta

}
